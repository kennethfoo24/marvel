apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: datadog-agent-claim2
  name: datadog-agent-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
